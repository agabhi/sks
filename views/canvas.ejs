<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Upload and Display on Canvas</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    
    <style>
        /* Reset CSS */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Raleway', sans-serif;
            background-color: #f5f5f5;
            display: flex;
            flex-direction: column;
            height: 100vh;
        }

        header {
            background-color: #fff;
            padding: 20px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            flex-shrink: 0;
        }

        nav {
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .logo {
            font-size: 24px;
            font-weight: bold;
        }

        .nav-links {
            list-style-type: none;
            display: flex;
        }

        .nav-links li {
            margin-right: 20px;
        }

        .nav-links a {
            text-decoration: none;
            color: #333;
        }

        h1 {
            font-family: 'Lobster', cursive;
            font-size: 2.5em;
            margin-bottom: 20px;
            color: #4a90e2;
        }

        body .container {
            display: flex;
            flex: 1;
            max-width: 100%;
            background-color: white;
            overflow: hidden;
            margin-left: 50px;
            padding-left: 0px;
        }

        .left-panel {
            width: calc(30% - 50px); /* Adjust for the width of optionsPanel */
            background-color: #fafafa;
            display: flex;
            flex-direction: column;
            align-items: center;
            border-right: 1px solid #ddd;
            height: 100%;
            padding-top: 20px; /* Ensure space for the header */
            padding-left: 10px;
            padding-right: 10px;
        }

        .right-panel {
            width: 70%;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
        }

        #optionsPanel {
            display: flex;
            flex-direction: column;
            align-items: center;
            width: 50px;
            height: calc(100vh - 100px); /* Adjusted height to account for header */
            border-right: 1px solid #ddd;
            background-color: #f1f1f1;
            padding-top: 20px;
            position: fixed; /* Ensure options panel is fixed on the left */
            left: 0;
            top: 100px; /* Adjust according to the header height */
        }

        #optionsPanel button {
            background: none;
            border: none;
            cursor: pointer;
            font-size: 1.5em;
            color: #4a90e2;
            margin: 10px 0;
        }

        .controls {
            width: 100%;
            padding-left: 10px; /* Adjust for options panel width */
        }

        input[type="file"], input[type="color"], textarea, select, button {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            margin-bottom: 10px;
            transition: box-shadow 0.3s ease;
        }

        input[type="file"]:hover, button:hover {
            background: #f1f1f1;
        }

        textarea {
            height: 80px;
            resize: none;
        }

        button {
            background: #4a90e2;
            color: #fff;
            border: none;
            cursor: pointer;
            font-size: 1em;
        }

        canvas {
            border: 2px solid #4a90e2;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        #imageOptions, #textOptions {
            display: none;
            width: 100%;
        }

        .slide-down {
            display: none;
            position: fixed;
            top: 60px; /* Adjust according to the header height */
            left: 0;
            width: 100%;
            background-color: #f1f1f1;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            z-index: 10;
            padding: 10px;
            height:50px;
        }

        .slide-down.show {
            display: block;
        }

        .icon-buttons {
            display: flex;
            flex-direction: row;
            justify-content: space-around;
            width: 100%;
        }

        .icon-button {
            background: none;
            border: none;
            cursor: pointer;
            font-size: 0.9em;
            color: #4a90e2;
        }

        .icon-button i {
            margin-right: 5px; /* Space between icon and text */
        }

        .template-panel {
            display: none;
            position: fixed;
            top: 0;
            right: 0;
            width: 300px;
            height: 100%;
            background-color: #fff;
            box-shadow: -2px 0 5px rgba(0, 0, 0, 0.1);
            z-index: 20;
            overflow-y: auto;
            padding: 20px;
        }

        .template-panel.show {
            display: block;
        }

        .template-panel .close-btn {
            position: absolute;
            top: 10px;
            right: 10px;
            cursor: pointer;
            font-size: 1.5em;
            color: #333;
        }

        .template-thumbnail {
            width: 100%;
            height: auto;
            margin-bottom: 10px;
            cursor: pointer;
        }

        .change-template-btn {
            position: absolute;
            top: 10px;
            right: 10px;
            cursor: pointer;
            font-size: 1em;
            background-color: #4a90e2;
            color: #fff;
            border: none;
            padding: 5px 10px;
            border-radius: 5px;
        }
        
    </style>
    
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Lobster|Pacifico|Raleway|Roboto|Open+Sans">
</head>
<body>
    <!-- Including header section using EJS syntax -->
    <%- include('home/header') %>
    <%- include('canvas/canvas_inner') %>
</body>
</html>
